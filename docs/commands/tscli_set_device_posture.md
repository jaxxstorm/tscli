<!-- Code generated by `make docs-generate`; DO NOT EDIT. -->

## tscli set device posture

Set custom posture attributes on a device

### Synopsis

Set one or more custom posture attributes on a specific device.

Examples:

  # Set a string attribute
  tscli set device posture --device node-abc123 --key custom:group --value production

  # Set a boolean attribute  
  tscli set device posture --device node-abc123 --key custom:compliant --value true

  # Set an integer attribute
  tscli set device posture --device node-abc123 --key custom:score --value 95

  # Set an attribute with expiry
  tscli set device posture --device node-abc123 --key custom:temp --value test --expiry 2024-12-31T23:59:59Z


```
tscli set device posture [flags]
```

### Options

```
      --comment string   Optional audit-log comment
      --device string    Device ID
      --expiry string    Expiry time in RFC3339 format (optional)
  -h, --help             help for posture
      --key string       Posture attribute key (must start with 'custom:')
      --value string     Posture attribute value
```

### Options inherited from parent commands

```
  -k, --api-key string   Tailscale API key
  -d, --debug            Dump HTTP requests/responses
  -o, --output string    Output: [human json pretty yaml]
  -n, --tailnet string   Tailscale tailnet (default "-")
```

### SEE ALSO

* [tscli set device](tscli_set_device.md)	 - Set device commands

